import React from 'react';

/**
 * Legacy canvas (butterflies + glitter) removed — the dashboard shell uses
 * `CosmicDashboardBackground` (Milky Way, nebulae, planets, constellations).
 * This component is kept as a no-op so any future import does not break.
 */
export function SoulBackground() {
  return null;
}

export default SoulBackground;
